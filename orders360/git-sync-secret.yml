name: git-sync-mrab
version: v2alpha
type: secret
workspace: system
layer: user
description: "Secret for git-sync authentication. Set GITSYNC_PASSWORD when applying; never commit tokens."
secret:
  type: key-value
  data:
    GITSYNC_USERNAME: "mrabhishekb"
    GITSYNC_PASSWORD: ""
